# Diffusion Chat 기능 개발 로드맵

## ✅ 완료된 작업 (Phase 1)

### 1. 기본 UI 구현
- [x] 채팅 인터페이스 생성
- [x] 프롬프트 입력 폼
- [x] 네거티브 프롬프트 입력
- [x] 고급 설정 사이드바
- [x] 이미지 표시 및 다운로드 기능

### 2. API 통합
- [x] Diffusion API 클라이언트 구현
- [x] 이미지 생성 요청/응답 처리
- [x] 에러 핸들링
- [x] 로딩 상태 관리

## 🚧 다음 단계 (Phase 2)

### 1. 사용자 경험 개선

#### 1.1 실시간 생성 상태
- [ ] 진행률 표시 (Progress bar)
- [ ] 예상 소요 시간 표시
- [ ] 생성 단계별 상태 메시지
- [ ] 취소 기능 추가

#### 1.2 이미지 미리보기 개선
- [ ] 이미지 확대/축소 (Zoom)
- [ ] 이미지 비교 기능 (이전 생성과 비교)
- [ ] 이미지 편집 기능 (크롭, 필터 등)
- [ ] 이미지 갤러리 뷰

#### 1.3 프롬프트 도우미
- [ ] 프롬프트 템플릿 제공
- [ ] 자동완성 기능
- [ ] 프롬프트 히스토리 저장
- [ ] 인기 프롬프트 추천

### 2. 고급 기능

#### 2.1 이미지 변형
- [ ] 이미지 투 이미지 (img2img)
- [ ] 이미지 인페인팅 (Inpainting)
- [ ] 이미지 아웃페인팅 (Outpainting)
- [ ] 스타일 전이 (Style Transfer)

#### 2.2 배치 생성
- [ ] 여러 이미지 동시 생성
- [ ] 프롬프트 변형 자동 생성
- [ ] 그리드 뷰로 결과 확인

#### 2.3 모델 선택
- [ ] 여러 모델 지원 (SDXL, SD 1.5, 등)
- [ ] 모델별 프리셋 제공
- [ ] 커스텀 모델 업로드

### 3. 데이터 관리

#### 3.1 히스토리 관리
- [ ] 생성 이력 저장 (로컬/서버)
- [ ] 히스토리 검색 및 필터링
- [ ] 즐겨찾기 기능
- [ ] 히스토리 공유 기능

#### 3.2 사용자 계정 연동
- [ ] 로그인 사용자 히스토리 저장
- [ ] 클라우드 동기화
- [ ] 사용량 제한 관리
- [ ] 구독 플랜 연동

### 4. 성능 최적화

#### 4.1 캐싱
- [ ] 동일 프롬프트 결과 캐싱
- [ ] 이미지 CDN 연동
- [ ] 썸네일 생성 및 캐싱

#### 4.2 최적화
- [ ] 이미지 압축 옵션
- [ ] 저해상도 미리보기 → 고해상도 다운로드
- [ ] 백그라운드 생성 (Queue 시스템)

### 5. 통합 및 확장

#### 5.1 다른 서비스와 통합
- [ ] YOLO 서비스와 연동 (이미지 분석)
- [ ] Chatbot 서비스와 연동 (대화형 생성)
- [ ] 파일 업로드 서비스와 연동

#### 5.2 API Gateway 통합
- [ ] API Gateway를 통한 라우팅
- [ ] 인증/인가 추가
- [ ] Rate limiting 적용
- [ ] 모니터링 및 로깅

## 📋 구현 우선순위

### High Priority (즉시 구현)
1. ✅ 기본 UI 및 API 통합
2. 🔄 실시간 생성 상태 표시
3. 🔄 이미지 확대/축소 기능
4. 🔄 프롬프트 히스토리 저장

### Medium Priority (1-2주 내)
1. 이미지 투 이미지 기능
2. 배치 생성 기능
3. 히스토리 관리 시스템
4. 사용자 계정 연동

### Low Priority (장기 계획)
1. 커스텀 모델 업로드
2. 고급 편집 기능
3. 클라우드 동기화
4. 구독 플랜 시스템

## 🔧 기술 스택 고려사항

### 프론트엔드
- **이미지 처리**: `react-image-gallery`, `react-image-zoom`
- **상태 관리**: `zustand` 또는 `jotai` (간단한 상태)
- **캐싱**: `react-query` 또는 `swr`
- **파일 다운로드**: `file-saver`

### 백엔드 통합
- **API Gateway**: Spring Cloud Gateway
- **인증**: JWT 또는 쿠키 기반
- **스토리지**: S3 또는 로컬 파일 시스템
- **큐 시스템**: Redis Queue (선택)

### 모니터링
- **에러 추적**: Sentry
- **성능 모니터링**: Vercel Analytics
- **사용량 통계**: 커스텀 대시보드

## 📝 구현 가이드

### Phase 2.1: 실시간 상태 표시

```typescript
// lib/api/diffusion.ts에 추가
export async function generateImageWithProgress(
  request: GenerateImageRequest,
  onProgress?: (progress: number) => void
): Promise<GenerateImageResponse> {
  // WebSocket 또는 Server-Sent Events 사용
  // 또는 폴링 방식으로 진행률 확인
}
```

### Phase 2.2: 히스토리 관리

```typescript
// lib/storage/diffusion-history.ts
export interface DiffusionHistory {
  id: string;
  prompt: string;
  imageUrl: string;
  settings: GenerateImageRequest;
  createdAt: Date;
}

export function saveHistory(history: DiffusionHistory): void {
  // localStorage 또는 API 호출
}

export function getHistory(): DiffusionHistory[] {
  // 저장된 히스토리 불러오기
}
```

### Phase 2.3: API Gateway 통합

```typescript
// lib/api/diffusion.ts 수정
const DIFFUSION_API_BASE_URL =
  process.env.NEXT_PUBLIC_API_BASE_URL || 'http://localhost:8080';

// Gateway를 통한 라우팅
export async function generateImage(request: GenerateImageRequest) {
  const response = await fetch(
    `${DIFFUSION_API_BASE_URL}/api/cv/diffusion/generate`,
    // ...
  );
}
```

## 🎯 성공 지표

1. **사용자 만족도**: 생성 시간, 이미지 품질
2. **성능**: 평균 생성 시간, 에러율
3. **사용량**: 일일 생성 횟수, 활성 사용자
4. **기능 활용도**: 고급 설정 사용률, 히스토리 저장률

## 📚 참고 자료

- [Diffusers 공식 문서](https://huggingface.co/docs/diffusers)
- [Stable Diffusion 가이드](https://stability.ai/stable-diffusion)
- [Next.js API Routes](https://nextjs.org/docs/api-routes/introduction)

