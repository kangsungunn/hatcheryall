# EC2 Docker 배포 가이드

## 진행 상황 요약

- ✅ Docker Hub에 이미지 빌드 및 푸시 완료 (`kangsungunn/api-hatchery-kr:v1`)
- ✅ EC2 인스턴스 생성 및 접속 완료
- ✅ EC2에 Docker 설치 완료
- ✅ Docker Hub 연결 테스트 완료
- ⏳ EC2에 `.env` 파일 생성 필요
- ⏳ Docker Hub 이미지 실행 준비 중

---

## 1. Docker Hub 이미지 정보

**이미지 이름:** `kangsungunn/api-hatchery-kr:v1`

**Docker Hub URL:** https://hub.docker.com/r/kangsungunn/api-hatchery-kr

**이미지 특징:**
- Gateway 서비스 포함
- 포트: 8080
- 환경변수 기반 설정 (Redis, Database 등)
- Upstash Redis 사용 가능

---

## 2. EC2 인스턴스 정보

**인스턴스 접속:**
```bash
ssh -i "spring.pem" ubuntu@ec2-3-34-1-145.ap-northeast-2.compute.amazonaws.com
```

**인스턴스 사양:**
- OS: Ubuntu 24.04.3 LTS
- Docker: 29.1.3
- Docker Compose: v5.0.0
- 사용자: ubuntu

---

## 3. EC2 Docker 설치 완료 내역

### 3-1. 시스템 업데이트
```bash
sudo apt update && sudo apt upgrade -y
```

### 3-2. Docker 설치
```bash
curl -fsSL https://get.docker.com | sudo sh
```

### 3-3. 사용자 권한 설정
```bash
sudo usermod -aG docker $USER
exit
# 재접속 필요
```

### 3-4. 설치 확인
```bash
docker --version
docker compose version
docker run hello-world
```

**설치 결과:**
- ✅ Docker Engine 29.1.3 설치 완료
- ✅ Docker Compose v5.0.0 포함
- ✅ Docker Hub 연결 정상

---

## 4. 다음 단계: 환경변수 설정

### 4-1. EC2에 SSH 접속

**📍 실행 위치: Windows PowerShell (로컬 컴퓨터)**

```powershell
ssh -i "spring.pem" ubuntu@ec2-3-34-1-145.ap-northeast-2.compute.amazonaws.com
```

**참고:** `spring.pem` 파일이 있는 디렉토리에서 실행하거나, 전체 경로를 지정하세요.
예: `ssh -i "C:\Users\hi\Documents\spring.pem" ubuntu@ec2-3-34-1-145.ap-northeast-2.compute.amazonaws.com`

---

### 4-2. EC2에 `.env` 파일 생성

**📍 실행 위치: SSH 접속 후 (EC2 서버 내부)**

```bash
cd ~
nano .env
```

### 4-2. `.env` 파일 내용

```env
# Redis (Upstash)
REDIS_HOST=summary-polliwog-43960.upstash.io
REDIS_PORT=6379
REDIS_PASSWORD=2499DJlYTkwN2M5ZjNkZjQ0ZWRlOGIyMzZjNTc1YmI4YTcxMnAyNDM5NjA

# Database (Neon PostgreSQL)
SPRING_DATASOURCE_URL=jdbc:postgresql://your-db-host:5432/kroaddy
SPRING_DATASOURCE_USERNAME=your-username
SPRING_DATASOURCE_PASSWORD=your-password

# JWT
JWT_SECRET=your-jwt-secret-min-32-characters

# OAuth (필요시)
KAKAO_REST_API_KEY=your-key
KAKAO_CLIENT_SECRET=your-secret
NAVER_CLIENT_ID=your-id
NAVER_CLIENT_SECRET=your-secret
GOOGLE_CLIENT_ID=your-id
GOOGLE_CLIENT_SECRET=your-secret
```

**저장 방법 (nano 에디터 내부):**
- `Ctrl + X` → `Y` → `Enter`

**파일 권한 설정:**

**📍 실행 위치: SSH 접속 후 (EC2 서버 내부)**

```bash
chmod 600 .env
```

---

## 5. Docker Hub 이미지 실행 명령어

### 5-1. 이미지 Pull

**📍 실행 위치: SSH 접속 후 (EC2 서버 내부)**

```bash
docker pull kangsungunn/api-hatchery-kr:v1
# 또는
docker pull kangsungunn/api-hatchery-kr:latest
```

**이미지 확인:**

**📍 실행 위치: SSH 접속 후 (EC2 서버 내부)**

```bash
docker images | grep api-hatchery-kr
```

---

### 5-2. 컨테이너 실행

**📍 실행 위치: SSH 접속 후 (EC2 서버 내부)**

**기본 실행 (리소스 제한 없음):**

**한 줄로 입력:**
```bash
docker run -d -p 8080:8080 --name api-hatchery-kr --env-file .env --restart unless-stopped kangsungunn/api-hatchery-kr:v1
```

**여러 줄로 입력:**
```bash
docker run -d \
  -p 8080:8080 \
  --name api-hatchery-kr \
  --env-file .env \
  --restart unless-stopped \
  kangsungunn/api-hatchery-kr:v1
```

**리소스 제한 포함 실행 (권장):**

**방법 1: 한 줄로 입력 (가장 간단, 권장)**
```bash
docker run -d --name api-hatchery-kr -p 8080:8080 --env-file .env --restart unless-stopped --memory="1g" --cpus="1.5" kangsungunn/api-hatchery-kr:v1
```

**방법 2: 여러 줄로 입력 (가독성 좋음)**
```bash
docker run -d \
  --name api-hatchery-kr \
  -p 8080:8080 \
  --env-file .env \
  --restart unless-stopped \
  --memory="1g" \
  --cpus="1.5" \
  kangsungunn/api-hatchery-kr:v1
```

**⚠️ 여러 줄 입력 시 주의사항:**
- 각 줄 끝에 백슬래시(`\`)를 입력하고 **Enter**를 누르세요
- 백슬래시 뒤에 공백이 있으면 안 됩니다
- 마지막 줄에는 백슬래시를 붙이지 않습니다
- 한 줄로 입력하는 것이 더 간단하고 실수할 확률이 적습니다

**옵션 설명:**
- `-d`: 백그라운드 실행
- `--name api-hatchery-kr`: 컨테이너 이름
- `-p 8080:8080`: 포트 매핑 (호스트:컨테이너)
- `--env-file .env`: 환경변수 파일 사용
- `--restart unless-stopped`: 자동 재시작 설정
- `--memory="1g"`: 메모리 제한 1GB (선택사항)
- `--cpus="1.5"`: CPU 제한 1.5코어 (선택사항)

---

### 5-3. 실행 확인

**📍 실행 위치: SSH 접속 후 (EC2 서버 내부)**

```bash
# 컨테이너 상태 확인
docker ps

# 로그 확인
docker logs api-hatchery-kr

# 실시간 로그 확인 (종료: Ctrl + C)
docker logs -f api-hatchery-kr
```

---

### 5-4. 접속 테스트

**📍 실행 위치: SSH 접속 후 (EC2 서버 내부)**

```bash
curl http://localhost:8080/docs
```

**📍 실행 위치: Windows PowerShell (로컬 컴퓨터) 또는 브라우저**

```powershell
# PowerShell에서
curl http://ec2-3-34-1-145.ap-northeast-2.compute.amazonaws.com:8080/docs

# 또는 브라우저에서 직접 접속
# http://ec2-3-34-1-145.ap-northeast-2.compute.amazonaws.com:8080/docs
```

**⚠️ 주의:** 외부 접속이 안 되면 AWS 보안 그룹에서 8080 포트를 열어야 합니다.

---

## 6. 배포 스크립트 (선택사항)

### 6-1. 배포 스크립트 생성

**📍 실행 위치: SSH 접속 후 (EC2 서버 내부)**

EC2에서 `deploy.sh` 파일 생성:

```bash
nano deploy.sh
```

**스크립트 내용:**
```bash
#!/bin/bash

echo "=== Starting Deployment ==="

# 최신 이미지 pull
echo "Pulling latest image from Docker Hub..."
docker pull kangsungunn/api-hatchery-kr:latest

# 기존 컨테이너 중지 및 삭제
echo "Stopping existing container..."
docker stop api-hatchery-kr 2>/dev/null || true
docker rm api-hatchery-kr 2>/dev/null || true

# 새 컨테이너 실행
echo "Starting new container..."
docker run -d \
  --name api-hatchery-kr \
  -p 8080:8080 \
  --env-file .env \
  --restart unless-stopped \
  kangsungunn/api-hatchery-kr:latest

# 실행 확인
echo "Waiting for container to start..."
sleep 5

# 상태 확인
if docker ps | grep -q api-hatchery-kr; then
    echo "✅ Deployment successful!"
    docker ps | grep api-hatchery-kr
else
    echo "❌ Deployment failed!"
    docker logs api-hatchery-kr
    exit 1
fi
```

**실행 권한 부여:**

**📍 실행 위치: SSH 접속 후 (EC2 서버 내부)**

```bash
chmod +x deploy.sh
```

**스크립트 실행:**

**📍 실행 위치: SSH 접속 후 (EC2 서버 내부)**

```bash
./deploy.sh
```

---

## 7. 컨테이너 관리 명령어

### 7-1. 컨테이너 제어

**📍 실행 위치: SSH 접속 후 (EC2 서버 내부)**

```bash
# 재시작
docker restart api-hatchery-kr

# 중지
docker stop api-hatchery-kr

# 시작 (중지된 컨테이너)
docker start api-hatchery-kr

# 제거 (중지 후)
docker stop api-hatchery-kr
docker rm api-hatchery-kr

# 강제 제거 (중지하지 않고 바로 삭제)
docker rm -f api-hatchery-kr
```

---

### 7-2. 로그 관리

**📍 실행 위치: SSH 접속 후 (EC2 서버 내부)**

```bash
# 로그 확인
docker logs api-hatchery-kr

# 실시간 로그 (종료: Ctrl + C)
docker logs -f api-hatchery-kr

# 최근 100줄만
docker logs --tail 100 api-hatchery-kr
```

---

### 7-3. 이미지 관리

**📍 실행 위치: SSH 접속 후 (EC2 서버 내부)**

```bash
# 이미지 목록
docker images

# 특정 이미지 검색
docker images | grep api-hatchery-kr

# 이미지 삭제
docker rmi kangsungunn/api-hatchery-kr:v1

# 사용하지 않는 이미지 정리
docker image prune -a
```

---

## 8. AWS 보안 그룹 설정

**필수 포트:**
- SSH (22): 내 IP만 허용
- HTTP (8080): 애플리케이션 포트 (필요시 모든 IP 또는 특정 IP만)

**보안 그룹 설정 방법:**
1. AWS 콘솔 → EC2 → Security Groups
2. 인바운드 규칙 편집
3. 규칙 추가:
   - Type: Custom TCP
   - Port: 8080
   - Source: 0.0.0.0/0 (또는 특정 IP)

---

## 9. 문제 해결

### 9-1. 컨테이너가 바로 종료되는 경우

**📍 실행 위치: SSH 접속 후 (EC2 서버 내부)**

```bash
# 로그 확인
docker logs api-hatchery-kr

# 환경변수 확인
docker inspect api-hatchery-kr | grep -A 20 Env
```

---

### 9-2. 포트 접근 불가

**📍 실행 위치: SSH 접속 후 (EC2 서버 내부)**

```bash
# EC2 내부에서 포트 확인
sudo netstat -tlnp | grep 8080

# 방화벽 확인
sudo ufw status
```

**📍 실행 위치: AWS 콘솔 (웹 브라우저)**

- EC2 → Security Groups → 인바운드 규칙에서 8080 포트 확인

---

### 9-3. 이미지 pull 실패

**📍 실행 위치: SSH 접속 후 (EC2 서버 내부)**

```bash
# 네트워크 확인
ping docker.com

# Docker Hub 연결 확인
curl https://hub.docker.com
```

### 9-4. Dockerfile을 찾을 수 없는 경우
**에러 메시지:**
```
ERROR: failed to build: failed to solve: failed to read dockerfile: open Dockerfile: no such file or directory
```

**원인:**
- EC2에서 `docker build` 명령을 실행했지만, 현재 디렉토리에 Dockerfile이 없음
- 이미 Docker Hub에 이미지가 있으므로 EC2에서 빌드할 필요가 없음

**해결 방법:**

1. **권장 방법: Docker Hub에서 이미지 pull (빌드 불필요)**

   **📍 실행 위치: SSH 접속 후 (EC2 서버 내부)**
   
   ```bash
   docker pull kangsungunn/api-hatchery-kr:v1
   
   # 이미지 확인
   docker images | grep api-hatchery-kr
   ```

2. **로컬에서 빌드가 필요한 경우:**

   **📍 실행 위치: Windows PowerShell (로컬 컴퓨터)**
   
   ```powershell
   # 프로젝트 디렉토리로 이동 (Dockerfile이 있는 곳)
   cd C:\Users\hi\Documents\hatchery.kr\kroaddy_project_dacon\api.kroaddy.site\gateway
   docker build --tag kangsungunn/api-hatchery-kr:v1 .
   ```

**참고:**
- EC2에서는 일반적으로 이미 빌드된 Docker Hub 이미지를 pull해서 사용
- 로컬 개발 환경에서만 이미지를 빌드하고 Docker Hub에 푸시

---

## 10. 다음 작업 예정

- [ ] EC2에 `.env` 파일 생성
- [ ] Docker Hub 이미지 pull 및 실행
- [ ] 애플리케이션 정상 작동 확인
- [ ] GitHub Actions 워크플로우 설정 (자동 빌드/푸시)
- [ ] 배포 자동화 스크립트 최적화

---

## 11. 참고 사항

### 11-1. 환경변수 우선순위
1. `docker run -e` 옵션
2. `--env-file` 파일
3. `application.yaml`의 기본값

### 11-2. Upstash Redis 설정
- `REDIS_HOST`: Upstash 엔드포인트 (프로토콜 없이)
- `REDIS_PORT`: 6379
- `REDIS_PASSWORD`: Upstash 토큰

### 11-3. Docker Hub 이미지 특징
- 이미지 자체에는 환경변수 포함 안 됨
- 실행 시 환경변수 주입 필요
- 코드 변경 없으면 이미지 재빌드 불필요

---

## 12. 유용한 명령어 모음

**📍 실행 위치: SSH 접속 후 (EC2 서버 내부)**

```bash
# 전체 컨테이너 확인
docker ps -a

# 컨테이너 리소스 사용량
docker stats api-hatchery-kr

# 컨테이너 내부 접속
docker exec -it api-hatchery-kr bash

# 컨테이너 환경변수 확인
docker exec api-hatchery-kr env

# Docker 시스템 정보
docker system df
docker system info
```

---

**작성일:** 2026-01-02  
**마지막 업데이트:** 2026-01-02  
**상태:** 진행 중

